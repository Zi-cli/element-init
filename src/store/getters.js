const getters = {
    venue: state => state.base.venue,
    team: state => state.base.team,
    system: state => state.base.system,
    info: state => state.base.info,
    message: state => state.base.message
}

export default getters
